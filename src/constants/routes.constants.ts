/**
 * Application route constants for internal and external navigation.
 */
export const ROUTES = {
  INTERNAL: {
    SIGN_IN: "/sign-in",
    WELCOME: "/",
  },

  EXTERNAL: {
    // Add external URLs here as needed
  },
} as const;
